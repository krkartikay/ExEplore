/*function Vector( x, y ) {
    this.x = x;
    this.y = y;
}

Vector.fromPolar = function( angle, length ) {
    return new Vector(
        length * Math.cos( angle ),
        -length * Math.sin( angle )
    );
};*/